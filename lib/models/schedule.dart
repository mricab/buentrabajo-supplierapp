class Schedule {
  //Schedule data
  String weekday;
  String start;
  String end;

  Schedule(
    String weekday,
    String start,
    String end,
  ) {
    this.weekday = weekday;
    this.start = start;
    this.end = end;
  }
}
